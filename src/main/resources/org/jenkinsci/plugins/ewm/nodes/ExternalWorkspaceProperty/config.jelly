<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%Disk Pool Ref ID}" field="diskPoolRefId">
        <f:textbox/>
    </f:entry>
    <f:entry>
        <f:repeatableProperty field="diskNodes" add="${%Add Disk}" header="${%Disk}"/>
    </f:entry>
</j:jelly>
