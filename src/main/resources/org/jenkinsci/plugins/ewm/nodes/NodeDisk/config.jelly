<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%Disk Ref ID}" field="diskRefId">
        <f:textbox/>
    </f:entry>
    <f:entry title="${%Local root path}" field="localRootPath">
        <f:textbox/>
    </f:entry>
    <f:entry>
        <div align="right">
            <f:repeatableDeleteButton value="${%Delete Disk}"/>
        </div>
    </f:entry>
</j:jelly>
