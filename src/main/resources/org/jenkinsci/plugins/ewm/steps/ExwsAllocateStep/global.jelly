<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="External Workspace Definitions">
        <f:entry>
            <f:hetero-list name="diskPools" items="${instance.diskPools}" descriptors="${descriptor.getDescriptors()}"
                           hasHeader="true" addCaption="Add Disk Pool"/>
        </f:entry>
    </f:section>
</j:jelly>
