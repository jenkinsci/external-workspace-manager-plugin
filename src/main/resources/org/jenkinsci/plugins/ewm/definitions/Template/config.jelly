<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%Node Label}" field="label">
        <f:textbox/>
    </f:entry>
    <f:entry>
        <f:repeatableProperty field="nodeDiskPools" add="${%Add Disk Pool}" header="${%Disk Pool}"/>
    </f:entry>
    <f:entry>
        <div align="right">
            <f:repeatableDeleteButton value="${%Delete Template}"/>
        </div>
    </f:entry>
</j:jelly>
