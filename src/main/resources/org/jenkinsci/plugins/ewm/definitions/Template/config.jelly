<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="Disk Ref ID" field="diskRefId">
        <f:textbox/>
    </f:entry>
    <f:entry title="Label" field="label">
        <f:textbox/>
    </f:entry>
    <f:entry>
        <f:hetero-list name="diskNodes" items="${instance.diskNodes}" descriptors="${descriptor.getDescriptors()}"
                       hasHeader="true" addCaption="Add Disk"/>
    </f:entry>
</j:jelly>
